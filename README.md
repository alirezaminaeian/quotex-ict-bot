# 🤖 ربات سیگنال ICT برای Quotex

ربات هوشمند تشخیص سیگنال‌های ICT (Inner Circle Trader) برای پلتفرم Quotex. این ربات فقط سیگنال‌های قوی را شناسایی می‌کند و به تلگرام ارسال می‌کند - **هیچ معامله‌ای خودکار انجام نمی‌دهد**.

---

## ✨ ویژگی‌ها

- ✅ **ذخیره سشن خودکار**: بعد از اولین ورود، دیگر نیاز به کد تأیید نیست
- ✅ **اسکن خودکار OTC**: تمام جفت‌های OTC را به صورت خودکار اسکن می‌کند
- ✅ **تشخیص ICT پیشرفته**: Order Block، FVG، Liquidity Sweep، Engulfing، BOS
- ✅ **فیلتر Kill Zone**: فقط در ساعات طلایی (Asia/London/New York) فعالیت می‌کند
- ✅ **ارسال سریع تلگرام**: سیگنال‌های قوی (≥85%) فوری به تلگرام ارسال می‌شوند
- ✅ **اجرای بلندمدت**: آماده اجرا روی سرور (VPS) با systemd/Task Scheduler

---

## 📋 پیش‌نیازها

- Python 3.12+
- Google Chrome (آخرین نسخه)
- حساب Quotex
- ربات تلگرام (از BotFather)

---

## 🚀 نصب سریع

### 1. کلون کردن repository

```bash
git clone https://github.com/YOUR_USERNAME/quotex-ict-bot.git
cd quotex-ict-bot
```

### 2. نصب کتابخانه‌ها

```bash
pip install -r requirements.txt
```

### 3. تنظیم فایل `.env`

فایل `.env.example` را کپی کنید و به `.env` تغییر نام دهید:

```bash
cp env.example .env
```

سپس فایل `.env` را با مقادیر واقعی پر کنید:

```env
QUOTEX_EMAIL=your_email@gmail.com
QUOTEX_PASSWORD=your_password
TELEGRAM_TOKEN=12345:ABCDEF
TELEGRAM_CHAT_ID=123456789
HEADLESS=false
```

### 4. ساخت ربات تلگرام

1. در تلگرام به `@BotFather` پیام دهید
2. دستور `/newbot` را بفرستید
3. `TELEGRAM_TOKEN` را در `.env` قرار دهید
4. برای `CHAT_ID`: به `@userinfobot` پیام دهید و عدد `id` را بردارید

### 5. ذخیره سشن Quotex

#### روش 1: استفاده از `login_helper.py` (ساده‌تر)

```bash
python login_helper.py
```

مرورگر باز می‌شود. وارد Quotex شوید و بعد از ورود به داشبورد، در ترمینال Enter بزنید.

#### روش 2: از Console مرورگر (بدون باز کردن مرورگر جدید)

1. در مرورگر خود به Quotex وارد شوید
2. Console را باز کنید (Ctrl+Shift+I)
3. کد زیر را اجرا کنید:

```javascript
(function(){
  const cookies = document.cookie.split('; ').map(c=>{
    const i = c.indexOf('=');
    return {name: c.substring(0,i), value: c.substring(i+1), domain: location.hostname, path: '/', secure: location.protocol==='https:'};
  });
  const ls = {};
  for (let i=0;i<localStorage.length;i++){
    const k = localStorage.key(i);
    ls[k] = localStorage.getItem(k);
  }
  const payload = {cookies, localStorage: ls};
  console.log('SESSION_JSON_START');
  console.log(JSON.stringify(payload));
  console.log('SESSION_JSON_END');
})();
```

4. JSON بین `SESSION_JSON_START` و `SESSION_JSON_END` را کپی کنید
5. در فایل `session_from_console.json` ذخیره کنید
6. تبدیل به فایل سشن:

```bash
python convert_session.py
```

### 6. اجرای ربات

```bash
python main.py
```

---

## 📊 منطق سیگنال ICT

### اندیکاتورهای استفاده شده:

- **Order Block (OB)**: کندل قوی قبل از برگشت (بدنه بزرگ، شدو کوچک)
- **Fair Value Gap (FVG)**: فاصله بین High کندل 1 و Low کندل 3 (یا برعکس)
- **Liquidity Sweep**: قیمت از سقف/کف اخیر رد شود و برگردد
- **Engulfing (M1)**: کندل فعلی کاملاً قبلی را بپوشاند
- **Break of Structure (BOS)**: شکست سقف/کف سوئینگ قبلی

### امتیازدهی همگرایی:

- OB + Sweep + Engulfing = **70%**
- + FVG = **80%**
- + BOS + Kill Zone = **85%+**

### انقضا:

- **پیشفرض**: 1 دقیقه
- **2 دقیقه**: فقط اگر شرایط قوی (≥85%) + ساعت 17:00–19:00 تهران

**فقط سیگنال‌های ≥85%** به تلگرام ارسال می‌شوند.

---

## ⏰ Kill Zone (ساعات فعالیت)

ربات فقط در این بازه‌ها (ساعت تهران) فعالیت می‌کند:

- **04:30 تا 07:30** - Asia OTC
- **11:30 تا 14:30** - London OTC
- **16:30 تا 19:30** - New York OTC (بهترین)

خارج از این ساعات، ربات منتظر می‌ماند.

---

## 🖥️ اجرا روی سرور (VPS)

برای راهنمای کامل نصب روی سرور، فایل `SERVER_SETUP.md` را مطالعه کنید.

### خلاصه (لینوکس):

```bash
# نصب خودکار
bash install_server.sh

# تنظیم .env و انتقال سشن
nano .env
# فایل session/quotex_session.pkl را بذار

# ساخت سرویس systemd
bash setup_systemd.sh

# شروع سرویس
sudo systemctl start quotex-ict-bot.service

# چک کردن وضعیت
sudo systemctl status quotex-ict-bot.service
```

---

## 📁 ساختار پروژه

```
quotex_ict_bot/
├── main.py                 # فایل اصلی ربات
├── login_helper.py         # ابزار ذخیره سشن
├── convert_session.py      # تبدیل JSON به سشن
├── test_telegram.py        # تست ارسال تلگرام
├── requirements.txt        # کتابخانه‌های Python
├── .env                    # تنظیمات (خودت بساز)
├── env.example             # نمونه تنظیمات
├── .gitignore              # فایل‌های نادیده گرفته شده
├── README.md               # این فایل
├── setup_guide.md          # راهنمای نصب فارسی
├── SERVER_SETUP.md         # راهنمای نصب روی سرور
├── install_server.sh       # اسکریپت نصب خودکار (Linux)
├── setup_systemd.sh        # ساخت سرویس systemd
├── install_server.ps1      # اسکریپت نصب (Windows)
├── session/                # پوشه سشن (خودت بساز)
│   └── quotex_session.pkl # فایل سشن (خودت بساز)
└── logs/                   # پوشه لاگ‌ها
    └── signals.log         # لاگ سیگنال‌ها
```

---

## 🔧 عیب‌یابی

### مشکل: لاگین ناموفق

- فایل `.env` را چک کنید (ایمیل/رمز درست است؟)
- فایل `session/quotex_session.pkl` موجود است؟
- اگر سشن منقضی شده، دوباره از `login_helper.py` استفاده کنید

### مشکل: پیام تلگرام نمی‌رسد

- `TELEGRAM_TOKEN` و `CHAT_ID` را چک کنید
- به ربات خود در تلگرام یک پیام "start" بدهید

### مشکل: سیگنال نمی‌فرستد

- مطمئن شوید در Kill Zone هستید
- چک کنید کندل‌ها از چارت خوانده می‌شوند (ممکنه UI تغییر کرده باشد)

### مشکل: مرورگر باز نمی‌شود (Linux)

```bash
sudo apt install -y libxss1 libappindicator1 libindicator7
sudo apt install -y libgconf-2-4 libxrandr2 libasound2
```

---

## 📝 لاگ‌ها

- **سیگنال‌ها**: `logs/signals.log`
- **خطاها**: `logs/bot_error.log` (روی سرور)

---

## ⚠️ نکات امنیتی

1. **هرگز فایل `.env` را commit نکنید** (در `.gitignore` است)
2. **فایل `session/quotex_session.pkl` را commit نکنید** (حاوی کوکی‌های شماست)
3. روی سرور: `HEADLESS=true` بگذارید

---

## 📄 مجوز

این پروژه برای استفاده شخصی است. استفاده تجاری نیاز به مجوز دارد.

---

## 🤝 مشارکت

اگر مشکلی پیدا کردید یا پیشنهادی دارید، Issue یا Pull Request بفرستید.

---

## 📞 پشتیبانی

برای سوالات و مشکلات:
1. Issue در GitHub باز کنید
2. لاگ‌ها را بررسی کنید (`logs/signals.log`)

---

**موفق باشید و سیگنال‌های طلایی! 🚀📈**

